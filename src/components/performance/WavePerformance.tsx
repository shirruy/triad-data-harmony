import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export const WavePerformance = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Wave Performance</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Wave performance metrics will be displayed here.</p>
      </CardContent>
    </Card>
  )
}